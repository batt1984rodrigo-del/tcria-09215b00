name: MCP Gateway CI

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest

    steps:
      - name: Archived workflow notice
        run: |
          echo "MCP validation was consolidated into .github/workflows/main.yml."
          echo "Use the canonical CI workflow for dependency installation, tests, MCP validation, and container smoke checks."
