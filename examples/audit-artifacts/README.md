# Sample audit artifacts

This directory stores curated sample outputs that were previously kept in the repository root.

Purpose:

- preserve representative JSON, Markdown, CSV, and PDF outputs;
- keep the repository root visually clean;
- separate generated examples from source code and operational scripts;
- make demos and reviews easier without changing the current architecture.

These files are examples and historical references, not the required runtime location for newly generated outputs.
